
<h3></u>Send  the following contents</u></h3>
<%= form_with(model: @blog, url: blogs_path, local: true) do |form| %>
  <table border="2" class="comfirm">
  <%= form.hidden_field :content %>
  <tr><p class="comfirm"><td class="tt"><%= @blog.content %></p></td></tr>
  </table>
  <%= form.submit  "submit", class:"comfirm" %>
<% end %>
  <%= form_with(model: @blog, url: new_blog_path,local: true,  method: 'get') do |form| %>
  <%= form.hidden_field :content %>
  <%= form.submit "back", name: 'back', class:"comfir" %>
  
  <% end %>
